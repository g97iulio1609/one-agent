/**
 * OneAgent SDK Version
 *
 * Single source of truth for the SDK version.
 * All files should reference this constant instead of hardcoding version strings.
 */
export const SDK_VERSION = '4.2.0-alpha.0' as const;
export const SDK_MAJOR = 4 as const;
export const SDK_MINOR = 2 as const;
